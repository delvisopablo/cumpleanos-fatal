{
  "name": "cumpleanos-fatal",
  "version": "1.0.0",
  "private": true,
  "description": "Web de cumpleaños en 3D. Sitio estático, sin build.",
  "scripts": {
    "build": "echo \"Sitio estatico, no hay build\"",
    "start": "serve -s . -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
