# Frases de los invitados

`phrases.json` contiene los 16 invitados con ids `npc-01` a `npc-16`. Cada
entrada tiene un nombre y una lista `phrases`. Puedes cambiar las frases sin
tocar el código de la escena.
